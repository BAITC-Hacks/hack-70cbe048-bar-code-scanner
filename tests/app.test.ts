import {describe,it,expect,vi} from 'vitest'; import {lookup} from '../server/src/galmart.js';
const item={id:163043,title:'Food Master молоко 1000 мл',brand_name:'Food Master',pack_name:'ТЕТРАПАК',price:1265,old_price:null,unit:'шт',unit_value:1,unit_price:1265,available:true,inventory:null}; const response=(body:any,status=200)=>Promise.resolve(new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json'}}));
describe('validation and adapter',()=>{it('normalizes found result and City 2',async()=>{const f=vi.fn().mockImplementation((url:string,init:any)=>{expect(url).toContain('4870207314301');expect(init.headers.City).toBe('2');return response({data:[item]})});const r=await lookup('4870207314301','astana',f);expect(r.status).toBe('found');expect((r as any).source_updated_at).toBeNull()});it('returns not_found',async()=>{const r=await lookup('4870207314530','astana',()=>response({data:[]}));expect(r.status).toBe('not_found')});it('separates source errors and missing fields',async()=>{expect((await lookup('4870207314301','almaty',()=>response({},200))).status).toBe('source_error');expect((await lookup('4870207314301','almaty',()=>response({},503))).status).toBe('source_error')});it('passes City 1',async()=>{const f=vi.fn().mockImplementation((_u:string,i:any)=>{expect(i.headers.City).toBe('1');return response({data:[item]})});await lookup('4870055002696','almaty',f)})});
